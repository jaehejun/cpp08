#ifndef MUTANTSTACK_HPP
#define MUTANTSTACK_HPP

#include <stack>

template <typename T>
class MutantStack
{
	private:
		//std::stack mutantStack;
		//iterator iterator
	
	public:
		MutantStack();
		push()
		pop()
		iterator it
};

#endif